{
  "configurable": {
    "ai_model": "openai:gpt-4o-mini",
    "database": "AIC",
    "collection": "remit_recomm",
    "text_paths": ["platform_name", "recommendation_reason"],
    "vector_path": "embedding",
    "embedding_method": "embedding:openai:text-embedding-3-small",
    "system_prompt": "You are a helpful QA assistant over a MongoDB collection. For each question, call the mongodb_hybrid_search tool with the user's question as the query. The tool returns markdown in `results.format_results.markdown`. Use that markdown to answer the user. If the search results are empty or unrelated, say you do not know."
  }
}
